---
import { MessageCircle, Menu } from 'lucide-react';
---

<header class="header">
  <div class="container header-container">
    <a href="#" class="brand-logo-link">
      <img 
        src="/logo-signature-creme.png" 
        alt="Tomás Pussetto - Fuerza, hábitos y equilibrio" 
        class="header-signature-logo"
      />
      <span class="header-tagline font-bold">Fuerza, hábitos y equilibrio</span>
    </a>

    <nav class="nav-desktop">
      <a href="#filosofia" class="nav-link">Filosofía</a>
      <a href="#ciclo" class="nav-link">El Ciclo</a>
      <a href="#quiz" class="nav-link">Test Adherencia</a>
      <a href="#historias" class="nav-link">Historias</a>
      <a href="#asesoria" class="nav-link">Asesoría</a>
    </nav>

    <div class="header-actions">
      <a 
        href="https://wa.me/5493410000000?text=Hola%20Tomás,%20quiero%20información%20sobre%20tu%20asesoría%20integral" 
        target="_blank" 
        rel="noopener noreferrer"
        class="btn btn-primary btn-nav"
      >
        <MessageCircle size={18} />
        <span>Hablar con Tomás</span>
      </a>
      
      <button class="mobile-toggle" aria-label="Abrir menú" id="menuToggle">
        <Menu size={24} />
      </button>
    </div>
  </div>

  <!-- Mobile Drawer Nav -->
  <div class="mobile-drawer" id="mobileDrawer">
    <nav class="nav-mobile">
      <a href="#filosofia" class="mobile-link">Filosofía</a>
      <a href="#ciclo" class="mobile-link">El Ciclo</a>
      <a href="#quiz" class="mobile-link">Test Adherencia</a>
      <a href="#historias" class="mobile-link">Historias</a>
      <a href="#asesoria" class="mobile-link">Asesoría</a>
      <a 
        href="https://wa.me/5493410000000?text=Hola%20Tomás,%20quiero%20información%20sobre%20tu%20asesoría%20integral" 
        target="_blank" 
        rel="noopener noreferrer"
        class="btn btn-primary btn-mobile-cta"
      >
        <MessageCircle size={20} />
        <span>Hablar por WhatsApp</span>
      </a>
    </nav>
  </div>
</header>

<script>
  const toggle = document.getElementById('menuToggle');
  const drawer = document.getElementById('mobileDrawer');
  const links = document.querySelectorAll('.mobile-link');

  if (toggle && drawer) {
    toggle.addEventListener('click', () => {
      drawer.classList.toggle('active');
    });

    links.forEach(link => {
      link.addEventListener('click', () => {
        drawer.classList.remove('active');
      });
    });
  }
</script>

<style>
  .header {
    position: sticky;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    background: rgba(28, 26, 23, 0.92);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border-bottom: 1px solid var(--border-warm);
  }

  .header-container {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 85px;
  }

  .brand-logo-link {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    text-decoration: none;
  }

  .header-signature-logo {
    height: 38px;
    width: auto;
    object-fit: contain;
  }

  .header-tagline {
    font-size: 0.68rem;
    color: var(--color-creme-muted);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    margin-top: 2px;
  }

  .nav-desktop {
    display: flex;
    align-items: center;
    gap: 2rem;
  }

  .nav-link {
    color: var(--color-creme-muted);
    text-decoration: none;
    font-size: 0.95rem;
    font-weight: 500;
    transition: color 0.2s ease;
  }

  .nav-link:hover {
    color: var(--color-creme);
  }

  .header-actions {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .btn-nav {
    padding: 0.65rem 1.3rem;
    font-size: 0.9rem;
  }

  .mobile-toggle {
    display: none;
    background: transparent;
    border: none;
    color: var(--color-creme);
    cursor: pointer;
    padding: 0.5rem;
  }

  .mobile-drawer {
    display: none;
    position: fixed;
    top: 85px;
    left: 0;
    right: 0;
    background: var(--color-obsidian-dark);
    border-bottom: 1px solid var(--border-warm);
    padding: 1.5rem;
    transform: translateY(-100%);
    opacity: 0;
    transition: all 0.3s ease;
    pointer-events: none;
  }

  .mobile-drawer.active {
    transform: translateY(0);
    opacity: 1;
    pointer-events: auto;
  }

  .nav-mobile {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .mobile-link {
    color: var(--color-creme);
    text-decoration: none;
    font-size: 1.1rem;
    font-weight: 600;
  }

  .btn-mobile-cta {
    width: 100%;
    margin-top: 0.5rem;
  }

  @media (max-width: 900px) {
    .nav-desktop {
      display: none;
    }
    .mobile-toggle {
      display: block;
    }
    .mobile-drawer {
      display: block;
    }
    .btn-nav {
      display: none;
    }
  }
</style>
